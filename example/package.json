{
  "name": "warp-ipc-box-example",
  "version": "0.0.1",
  "description": "Inter-process component container",
  "main": "./index.js",
  "scripts": {
    "build": "rm -rf ./dist && babel --source-maps=true --extensions='.es6' --copy-files -d ./dist ./",
    "start": "npm install && npm run build && cd ./dist && node ."
  },
  "dependencies": {
    "node-warp": "^1.x.x",
    "warp-ticker": "^0.x.x",
    "warp-ipc-box": "file:./../"
  },
  "devDependencies": {
    "babel": "^5.x.x",
    "babel-runtime": "^5.x.x"
  },
  "keywords": [
    "warp",
    "component",
    "ipc"
  ],
  "author": "Nikolay Chursin <n_chursin@head-point.ru>",
  "license": "ISC"
}
