{
  "name": "warp-ipc-box",
  "version": "0.0.2",
  "description": "Inter-process component container",
  "main": "./dist/index.js",
  "scripts": {
    "build": "rm -rf ./dist && babel --source-maps=true --extensions='.es6' --copy-files -d ./dist ./",
    "example": "cd ./example && npm install && cd .. && npm build && node ./dist/example"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/potrata/warp-ipc-box.git"
  },
  "dependencies": {
    "node-warp": "^1.x.x"
  },
  "devDependencies": {
    "babel": "^5.x.x",
    "babel-runtime": "^5.x.x"
  },
  "keywords": [
    "warp",
    "component",
    "ipc"
  ],
  "author": "Nikolay Chursin <nick.chursin@gmail.com>",
  "license": "ISC"
}
